# GitHub personal access token (fine-grained, read-only public repos)
# Used server-side only in /api — never exposed to the browser.
# Without a token, GitHub's unauthenticated rate limit is 60 req/hr per IP.
GITHUB_TOKEN=
